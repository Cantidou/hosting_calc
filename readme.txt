Для запуска тестов через docker-compose:
docker compose build
docker compose up

Посмотреть процесс можно по адресу: http://localhost:4444/ui
Результат Allure: http://localhost:5252/allure-docker-service-ui/projects/default